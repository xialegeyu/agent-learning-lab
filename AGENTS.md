# AGENTS.md

这个仓库用于学习 agent 开发。Codex 在这里工作时，请遵守以下约定。

## 项目原则

- 从零开始，小步实验。
- 不提前引入大型框架。
- 每次只学习一个核心概念。
- 代码和笔记放在同一个实验目录里，方便复盘。

## 实验结构

每个实验建议放在 `experiments/<number>-<topic>/`。

推荐包含：

- `README.md`：实验目标、运行方式、观察结果
- 源码文件：只放完成当前实验必需的代码
- `.env.example`：如果实验需要环境变量

## Codex 工作方式

- 在动手前先读当前实验的 `README.md`。
- 保持改动范围小。
- 优先让示例可以本地运行。
- 如果要安装依赖，先说明为什么需要。
- 不要把真实 API key、token 或私密配置提交进仓库。
